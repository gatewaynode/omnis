{
  "mcpServers": {
    "omnis": {
      "command": "${CLAUDE_PROJECT_DIR}/target/debug/omnis-mcp",
      "args": [
        "--root", "${CLAUDE_PROJECT_DIR}",
        "--log", "${CLAUDE_PROJECT_DIR}/.omnis/mcp.log"
      ]
    }
  }
}
