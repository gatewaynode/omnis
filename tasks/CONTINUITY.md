# Continuity notes

Written 2026-09-13 with the display rework (PRD D20) complete on branch `m5-tasks`, unpushed, awaiting the owner's fullscreen run on the 4K. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). `PRD.md` v0.4 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md`. M0–M4 merged (PRs #4, #5). The owner pushes and opens PRs; never push.
- Branch `m5-tasks`, HEAD `df98eee` plus the docs commit. On it: the cherry-picked M4 acceptance docs (`af6537b`), PRD D20 (`9893503`), and the display rework in eleven commits `7d0a527`…`df98eee` (see the TODO's "Display rework" block and its review). The tree is clean apart from the owner's `docs/background/backgrounds_and_skills.md`.
- **Verified at `df98eee`** (docs only after it): fresh `cargo metadata`; fmt; clippy workspace `--all-targets -D warnings` and `-p omnis-app --no-default-features` (library only: the test targets import the devtools-only `dev` module, pre-existing); `cargo test --workspace` 237 passed, 0 failed, 4 ignored; `lint-sim` (+ self-test) clean; no new duplicates; `validate packs/base packs/test`; replays `walk.ron` 30 commands → `c2b951a82b883485`, `fight.ron` 46 → `e4fe51018ea4939d` (moved with the pack hash a save records, not the fights); Sentrux rules pass, signal 7037; `OMNIS_DUMP_SCREENS` dumps at 1280×720; a `--window small` canvas capture.
- **Next**: the owner runs `cargo run -p omnis-app` fullscreen on the 4K (acceptance: 3×3 text pixels, the roster highlight following the turn, dice traces in the band log, the rat visible), then pushes `m5-tasks` and opens the PR. **Check that push's CI before anything else.** Then plan M5 (editor v1, `bevy_egui`) in plan mode: the TODO's M5 line, ARCH §8.4 and §6.1, PRD D4. Restart the MCP server on the new build before using it (it predates M4 and the resize).

## Facts the next work will lean on
- Layout (`crates/omnis-app/src/layout.rs`): inputs `CANVAS 1280×720`, `VIEWPORT_SIZE (960,540)` (`(u16,u16)`, the tileset's type), `CELL (6,8)`, map scales 8/16, `PAD_BUTTON (96,40)`, `MENU_ORIGIN (40,25)` with `MENU_COLUMNS 80`, `MENU_ROWS 16`; derived `VIEWPORT_COLUMNS 160`, `VIEWPORT_ROWS 67`, `MENU_BOX`, `RIGHT_COLUMN`, `SIDEBAR_MAP`, `HUD_LINES`, `PAD`, `BAND`; helpers `cell`, `menu_cell`, `row_y`, `rows`, `pad_button`; `Camera` const fns. `band.rs` owns the band's rows (`BAND_ROW0 68`, message row 0, captions row 2, six roster rows from 3, log column 63 × 18 rows, help row 21, `band_cell`). `combat_screen.rs`: header row 1, five stack rows from 3, `TOP_PANEL` rows 0–8, `COUNT_ROW 44` (from `actors::FRONT_FEET_Y 351`), bottom panel rows 60–66, action row 63; compile-time asserts hold the relations. Text budgets: `LONG_CELLS 100` (`LONG_CELLS <= band::LOG_CELLS`), `SHORT_CELLS 39` (unused by painters; a deletion horizon), `STACK_CELLS 55`, `DEFEAT_LINE_CELLS = LONG_CELLS`.
- Fit: `layout::fit(physical) -> Fit { scale, offset }`, `physical_to_canvas`, `canvas_translation`, wrappers `canvas_rect_to_window(rect, logical, scale_factor)` / `window_to_canvas`; `cursor::WindowSize { width, height, scale_factor }` from `WindowCreated` (reads the window's factor), `WindowResized`, `WindowScaleFactorChanged`; `pixel::outer_scale`, `RequestedWindow(Option<SizeClass>)`, `size_window` sets the class's physical size once. `SizeClass` and `SIZE_CLASSES` in `layout.rs`. `main.rs`: no `--window` → `WindowMode::BorderlessFullscreen(MonitorSelection::Current)`; unattended runs default to `small`.
- Tilesets: `packs/test/bake/{dungeon,outdoor}.ron` `viewport: (960, 540)`, `texel_scale: 4`; re-bake with `cargo run -p omnis-cli -- tileset bake <spec>`; `data/tiles/*.ron` are hashed into the pack fingerprint, so any bake needs `cargo test -p omnis-sim rebaseline -- --ignored` in the same commit; `plan::tests::every_tileset_is_baked_for_the_layouts_viewport` fails if the bake and `VIEWPORT_SIZE` ever land apart.
- Headless test kit unchanged (`tests/common/mod.rs`); `spot()` derives clicks from widget rects, so the integration suites did not care about the resize. Tests derive expectations from the constants (feet from `Camera::sy`, panels from `TOP_PANEL`/`BOTTOM_PANEL`, modal from `MODAL_*`); literal goldens only where the number is the specification (size classes, the 2× panel case).
- Sentrux: scan `crates/`, `max_fn_lines 100` incl. tests (`plan::viewport` is exactly 100: split it before any edit), `max_cycles 0`; no `omnis-app → omnis-data/core` imports (go through `omnis_sim::omnis_data`).
- Screen dumps: `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored` (PPM → `sips -s format png`). Capture: `cargo run -q -p omnis-app -- --seed 1 --no-dev-socket --window small --script party,party,forward,… --screenshot-canvas <png> --settle 40`.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons. Approved plans are the scope; wider fixes found on the way are follow-ups, or fixed with a test when a test fails on them and named in the commit.
- Verification after the last tree change with fresh `cargo metadata` when workspace contents change. One commit per checked item with the attribution trailer, gated on its checks with `&&` (LESSONS 2026-09-13); stage by name, never `add -A`; never push. The owner's files (`docs/background/*`, `.claude/`) are left alone.
- Ask before changing vision documents on drift; approved-by-plan doc edits are flagged in the commit. Check the last push's CI before planning the next milestone. First content is measured before the owner plays it.

## Open items and follow-ups
- Owner acceptance run, push, PR, CI. If the owner wants ARCH bumped to v0.3, do it in the PR branch.
- Follow-ups from the rework: raw `sim:message:*` keys now show in the log as well as the message line; the no-default-features clippy of test targets; `Line::short`/`SHORT_CELLS` deletion; a larger bitmap font; a scrollable log; more than one condition per roster row; word labels on the pad; `texel_scale 1` for finer bricks.
- Earlier horizons unchanged: saving from the pause overlay mid-fight; F5/F9 in a fight; healing downed members outside combat (M7); item loot and encounter budgets (M9); monster spellcasting and multiattack; `Surprise::Monsters`; surprise back on as data once played; harder placements once the party can rest and level; `screen.text` op; localized event text; CC0 art; finesse weapons; a `dead` condition for members to die; floating damage numbers (ARCH §8.1 still describes them); `--no-autostart` menu captures; curved pad arrows; hover moving the keyboard cursor; minimap click; `.omnis/mcp.log` rotation; rolled scores, hirelings, save list, in-game difficulty changes, portraits/icons, `depends` ranges, inns, Settler background, equipment slots. Socket re-audit of rhai 1.26.1 due 2026-10-10.
