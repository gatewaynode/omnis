# Continuity notes

Written 2026-09-12 after M3 "Party and characters", before the owner's acceptance run. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). World is Toel (`docs/background/introduction.md`). `PRD.md` v0.3 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md` milestones. M0–M3 done.
- Branch **`m3-tasks`**, 8 M2-review commits then the M3 chain: `afe2d84` omnis-expr; `62ee0c5` data schemas; `6b92454` packs/base; `04b009f` omnis-rules; `2300cc7` sim party/settings/schema 2; `92d853c` protocol and MCP; `807cf7e` app menus; then the docs commit. Nothing pushed; the owner opens PRs.
- **M3 is code-complete and verified headlessly**: 140 tests; clippy both app configs, fmt, `lint-sim`, `check-duplicates`, `validate packs/base packs/test`, golden replay, Sentrux rules pass. The `rules.set` half of "done when" is tested (wizard 4 points → `level * 10` → 10 points). **The screens have not been seen**: canvas captures cannot show the `bevy_ui` layer and window captures are black on this machine. **Next: the owner runs `cargo run -p omnis-app -- --window 1280x720` (no `--seed`, so the title shows), builds a six-member party, walks the test dungeon, and reports.** Expect layout tuning after that (font sizes, positions).
- After acceptance: M4 "Combat" (`tasks/TODO.md`). Base pack already holds goblin, skeleton, giant rat with attacks; `RollTrace` exists; `Party` has marching order with `front_row` from `rules/creation.ron`.

## Decisions and facts easy to get wrong after a compact
- Owner decisions for M3: point-buy only; races human, elf (high), dwarf (hill), halfling (lightfoot); classes fighter, cleric, wizard, rogue with SRD ids and SRD names; config panel = save rule (Anywhere | Relief | InnOnly) + permadeath, set on the New Game screen and fixed per save; backgrounds: Acolyte only, schema open, an Omnis "Settler" is the first candidate for later.
- `Command` is not `Copy` (drafts carry names): clone at call sites. `Rejection::{WrongMode, PartyFull, Character(CreationError), BadOrder}`. `PartyCommand::{Create(Draft), Reorder { order }}`; creation takes a turn but no time; a refused draft leaves `rngs` untouched (read the stream without inserting).
- `SAVE_SCHEMA = 2`; `World::from_ron` reads a `Header { schema }`, migrates 1 through a typed `WorldV1` (`crates/omnis-sim/src/migrate.rs`), fixture `tests/saves/v1.ron`. `World::new(data, seed, settings)`. `Replay { settings }`. Any `packs/test` or sim change → `cargo test -p omnis-sim rebaseline -- --ignored` in the same commit; the golden now records two packs (base, test) and the settings.
- Default pack lists everywhere: `packs/base` then `packs/test` (app `AppConfig::default`, CLI `packs_or_default`, MCP main, sim tests `common::data()`, CI replay step). Base has no maps; test provides the entry map and the asset source.
- Rules files: generic `slots` / `values` / `tables` (ARCH §5.5 corrected); slot names `spell_points.pool`, `spell_points.cost`, `hit_points.first_level`, `hit_points.per_level`; values `component_threshold 5`, `point_budget 27`, `score_min 8`, `score_max 15`, `party_slots 6`, `front_row 3`, `starting_food 10`; tables `point_cost`, `xp_thresholds`, `proficiency_bonus`. `modifier()` is Rust (SRD constant), not a slot.
- rhai 1.26.1 facts: `only_i64, no_float, sync, no_time, no_custom_syntax, no_function, no_closure, no_module, no_index, no_object, internals`, `default-features = false`; `set_max_expr_depths` takes ONE argument under `no_function`; call-level/array/map limits are compiled out; string literals cannot be disabled as symbols (the AST walk refuses `Expr::StringConstant`); `Expr::Variable` name at `.0.1` under `no_module`; operators are `FnCall` with `op_token: Some`; `Engine` is not `Clone` (`Rules::clone` rebuilds it); `Rules: PartialEq` by source. Depth error text: "Expression exceeds maximum complexity". `d()` reaches the caller's `Pcg32` through an `Arc<Mutex<Option<DiceState>>>` slot.
- serde/RON: fixed arrays (`[Ability; 2]`, `[u8; 6]`) are RON tuples `(..)`. Untagged `Reply`: `Value { value: Option<_> }` and `Done {}` match any object, so they stay last; new variants go before them. `Reply::Party { party }`, `Rules { rules }`, `Rule { rule }` are wrappers: JSON paths are `result.party.members`, `result.rule.source`.
- Protocol: 17 tools. `rules.set` is a host op (`is_host`), implemented in `Headless::handle` and `socket::handle`; `party.create` returns `Reply::Events`. `OpError::UnknownSlot`. A save forbidden by the rule is `OpError::Failed("the save rule forbids saving here")` on both hosts and a notice on F5. Socket with no world: "no game is running; start one from the menu or launch with --autostart".
- App: `AppConfig.autostart` (set by `--autostart`, `--seed`, `--script`, `--screenshot*`); `StartIn(PlayState)` resource read `OnEnter(AppState::Playing)`; `PlayState` default is `CreateParty`; `apply_commands` runs in Explore or CreateParty; Escape → `ShellCommand::Pause`. Menus: `menu.rs` (Bevy-free: `Title`, `NewGameForm`, `Catalog`, `CreationForm`, `Pause`, `MenuKey`), `menus.rs` (`MenusPlugin`, `Screens` resource, 24 text lines per screen, `DespawnOnExit`), `party_panel.rs` over `layout::SIDEBAR_PARTY = (244, 76, 72, 100)` via `canvas_rect_to_window`, font 7 × integer scale. Creation form rows: 0 name, 1 race, 2 class, 3 background, 4 alignment, 5..=10 scores, 11 skills (Left/Right moves, Enter toggles), 12 Add, 13 Begin. In headless tests a key needs two `app.update()`s (read, then the state applies).
- Viewport facts unchanged from M2: cone rows span `-(d+1)..=(d+1)`; sprite counts dungeon 317, outdoor 173; captures `cargo run -q -p omnis-app -- --seed 1 --window 1280x720 --no-dev-socket --script forward,forward --screenshot-canvas <png> --settle 40` (comma-separated words; `map`, `save`, `load` are shell words).
- Sentrux: scan `crates/`; `max_fn_lines 100` applies to tests; rules pass.
- Editing: read the current text before patching (LESSONS); rustfmt rewraps; `echo =====` fails in this zsh (equals expansion), use another separator.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons (`LESSONS.md`). Approved plans are the scope; wider fixes found on the way are reported as follow-ups.
- Verification runs only after the last tree change; fresh `cargo metadata` when workspace contents change. Commit per checked item with the attribution trailer; stage files by name (`git diff --name-only | xargs git add` plus new paths by name), never `add -A`; never push.
- Ask before changing vision documents on drift; factual corrections are flagged in the commit and the report (ARCH §4.1, §5.1, §5.2, §5.5, §8.1, §13 this milestone).

## Open items
- Owner acceptance of the M3 screens; likely font and layout tuning; the pixel font decision (ARCH has no row on fonts).
- `.omnis/mcp.log` grows with every request; rotate or truncate.
- Horizons from the M3 review: rolled scores, hirelings, a save list for Load, difficulty changes in-game, portraits and icons, `depends` version ranges, inns (Relief/InnOnly refuse every save until M7), the Settler background, equipment slots (all carried armor counts as worn), horizon band trapezoids (3 px sliver), CC0 monster and portrait art before M4.
- Socket re-audit of rhai 1.26.1 due 2026-10-10. Linux/Windows CI later; `.cargo/config.toml` when a fast linker exists; README rewrite when asked.
