# Continuity notes

Written 2026-09-19 after the stranded debug-item commit was recovered onto `m6c-tasks` and the README was rewritten; the branch is ready for the owner to push. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). `PRD.md` v0.4 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md`. M0–M6b merged to `main` (PRs #4, #5, #6; `main` = `96abe13`). The owner pushes and opens PRs; never push. The `gh` CLI is not installed here: CI results come from the owner.
- Branch **`m6c-tasks`** (off the PR #6 merge) holds M6c sensing: sim `df3a3a4`, app and MCP `d9aaf59`, docs and the layer cap `01f9aed`, continuity `05e2bf4`, then the cherry-pick `46b58a7` of the debug-menu pause item (originally `4e5973a` on local `m5-tasks`, made after PR #6 was pushed and so never merged; `m5-tasks` can be deleted once this branch is merged), and the README rewrite (the head). The tree is clean and nothing is pushed.
- **Owner instruction 2026-09-19: work the drift list next.** Take the drift items below one at a time: state what the vision document says, what is built, and the two ways to close each gap (change the document, or change the code), with a recommendation; edit `PRD.md` and `ARCHITECTURE.md` only on the owner's say-so per item, bump ARCH to v0.3 when done, and commit the docs as one "Docs: PRD and ARCH match M6 as built" commit (stage by name). M6c owner acceptance is still pending (build an Explorer, open a door, press LOOK or L, see the ring past the party's sight); ask whether they played it. Then M7 (town, services, rest, progression; PRD §13 closes the Phase 1 loop) is planned in plan mode after CI on the next push.

## Facts the next work will lean on
- Sensing as built: `omnis-sim/src/sense.rs` (`resolve`, `best_eyes`, `reach_for_layer`, `LAYERS` terrain and structure; a layer's reach is capped by the one below), `visibility::ray`, `layer::REMOTE`, the layer-aware `Automap::record`, `Event::Sensed { actor, item, checks: Vec<LayerCheck>, tiles: Vec<SensedTile> }`, `items::UseKind::{Heal, Sense}` (a sense use costs the source's minutes on the `sense` stream). App: `ShellCommand::Look` (L, `ToolButton::Look`), `look::look_command` (Bevy-free, the first spyglass of a member who can act), `sense_text::sense_line`, `plan::REMOTE_OUTLINE` ring at three pixels a tile or more, `tool_states(active, has_world, has_casts, has_members, has_look)`, `tool_for` returns a plain `ShellCommand`. Bridge: `compact_tiles` for `Visible` and `Sensed`.
- Items as built: `omnis-sim/src/items.rs` (`ItemCommand`, `UsePlan`, `validate_use`, `use_item`, `transfer`, `drop_worn`); `CombatCommand::Use`; events `Equipped`, `Unequipped`, `ItemMoved`, `ItemUsed`; `ops::ItemView`; MCP `Command` schema `oneOf` 9, combat arms 5, `item_schema` 6, `dev_schema` 12, tool count 18. App: `PlayState::Inventory`, `inventory_menu.rs`/`inventory_screen.rs`/`inventory.rs`, `use_menu.rs` (the fight's item picker, `WidgetId::Item`), `text.rs` (`Names`, `Line`, widths, roll math) under `combat_text.rs`, `spell_text.rs`, `item_text.rs`, `sense_text.rs`.
- Pause overlay: seven items at rows 8..14 (`Pause::DEBUG` = 4, dim with a notice when `menu::debug_available` says no; Quit to title `Row(5)`, Quit `Row(6)`). Casting and the debug menu as built: `combat/cast.rs`, `casting.rs`, `effects.rs`, `checks.rs::roll`, `combat/reaction.rs`, `utility.rs`, `visibility::depth`, `DevCommand` (12), `Settings.devtools`, `DebugPlugin` opens on `OnEnter(PlayState::Debug)` from the pause item or the backtick.
- Pins: base tuple `(4, 4, 3, 24, 16, 11, 3, 19)`; replays walk `238033710167572364`, fight `7317777168019603128` (untouched through M6b and M6c); `SAVE_SCHEMA 4`. Test count 348+ at the gate.
- Sentrux: `rescan` then `check_rules` before every commit; `max_fn_lines 100` incl. tests; at or near the cap `plan::viewport` 100 (untouched), `debug_menu::adjust` 93, `debug_screen::row_text` 91, `dump_screens` ~92; `screen.rs` 985 lines (move the screen-dump test out next). `scripts/verify.sh` is the gate: run it unpiped and check its status.
- Screen dumps: `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored` (`inventory`, `combat_use`, `pause` with seven items). Measurement: `cargo test -p omnis-sim --test measure -- --ignored --nocapture`. Restart the MCP server on a new build before using it.
- `README.md` now describes the layout, status table, build and run flags, controls, the gate, the MCP tools and licensing; it names M7 as next and lists the eighteen tools. Update its status table and controls line when a milestone lands.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons. Approved plans are the scope; wider fixes are follow-ups, or fixed with a test when a test fails on them and named in the commit. A deferred milestone keeps its number. Every action, dev tools included, gets a button or menu item before a key; no function keys; save by mouse is verified before a hand-over.
- Verification after the last tree change: `scripts/verify.sh > log 2>&1 && …`, never piped. One commit per checked item with the attribution trailer; stage by name, never `add -A`; never push. `.claude/` is never staged. Patch a file as it is after `cargo fmt` (anchors reflow; replace whole functions when in doubt; a python edit script is parsed whole and every anchor checked before anything is written; a script kept in the scratchpad is rerun after an anchor fix).
- Ask before changing vision documents on drift; approved-by-plan doc edits (the "as built" paragraphs in ARCH §4.5) are flagged in the commit. Check the last push's CI before planning the next milestone. First content is measured before the owner plays it. A display decision is measured on the owner's primary display.
- A commit made on a branch after its PR was pushed is stranded when the PR merges: after any merge, check `git log main..<old-branch>` before starting the next branch, and carry the tail over first.
- Dependencies: exact pins, 30 days old, N-1 for non-Bevy crates matching Bevy's version, Socket scores read before adding.

## Open items and follow-ups
- The drift list (the next work; not edited yet): PRD §8.3 action economy and reactions; PRD §8.1 vs the owner's crawler abilities; ARCH §8.1's plugin list gains `DebugPlugin`, `SheetPlugin`, `InventoryPlugin`; ARCH §9.3 says schemas are generated (hand-written in `omnis-mcp/src/schema.rs`); ARCH §4.1 `rng` vs `rngs`; `world.rs:149` Relief items in M6 (M7); the editor in Phase 1 (PRD D4/§13, ARCH §16); PRD §11.1 Feathers; two D20 rows; §14 depth 4. ARCH to v0.3.
- M6 horizons: objects and creatures as sensing layers, `Persistence::Expires`, other geometries, skills and divination as sources, cover and concealment as `sense.dc` inputs, a look from a fight; item counts other than one per move, item details, weight limits, proficiency and strength penalties, `doff_armor_minutes`; the sheet's attack line, death saves, traits pages, scrolling, age as a rules function; a seventh tool needs a wider column; MAP in a fight; no rest until M7; `--no-devtools`; the measurement policy is a fixed heuristic; raw `sim:message:*` keys; `Line::short` deletion; scrollable log; word labels on the pad; `texel_scale 1`; saving from the pause overlay mid-fight; loot and encounter budgets (M9); monster spellcasting; `Surprise::Monsters`; `screen.text` op; localized event text; CC0 art; finesse weapons; floating damage numbers; `.omnis/mcp.log` rotation. Socket re-audit of rhai 1.26.1 due 2026-10-10. The unused `bevy_egui = "=0.41.1"` pin stays for the editor.
