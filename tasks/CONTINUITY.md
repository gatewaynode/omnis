# Continuity notes

Written 2026-09-19 after the owner deferred the editor. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). `PRD.md` v0.4 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md`. M0–M4 merged (PRs #4, #5). The owner pushes and opens PRs; never push.
- Branch `m5-tasks` holds the M4 acceptance docs, the display rework, the wide canvas (accepted 2026-09-13 on the owner's 5120×1440 ultrawide) and now the editor deferral. The remote has the branch at `024772c`; CI runs on pushes to main and on pull requests only, so the branch's run starts with the PR (main is green at the M4 merge). The tree is clean apart from the owner's `docs/background/*` files (never staged).
- **2026-09-19 decision**: the editor (planned as M5 on 2026-09-13) waits. The plan, the dependency numbers and the object schema design are saved at `tasks/plans/editor-v1.md`; the TODO carries the editor as the first item of the Phases 2–5 block, ahead of the M9 procgen panel. M5 is now a vacant number; M6–M8 keep theirs because ARCHITECTURE cites M6 (casting) and the follow-ups cite M7 and M9. Renumbering is the owner's call.
- **Drift to raise with the owner** (vision documents are edited only after asking): PRD D4 ("First playable milestone: crawler loop plus editor"), PRD §13 Phase 1 ("Editor: tile paint … All Phase 1 content is built with it") and ARCH §16 row 1 still place the editor in Phase 1; PRD §11.1 says the editor "can use" `bevy_ui`/Feathers while A11 chose `bevy_egui`; the PRD has two rows numbered D20 and §14 still says "detail depth stays at 4" (the owner chose 6).
- **Next (owner's instruction before the compact, 2026-09-19): plan M6 right after the catchup.** Check CI first if a push happened (LESSONS rule), then enter plan mode for M6 (spells, items, remote sensing: casting in and out of combat, spell points and components with the D11 threshold, cantrips, item use and equipment, `Sense` with a spyglass feeding the automap as stale knowledge, D18), Planning inputs: the TODO M6 line and its done-when (a caster empties a spell pool in combat; a spyglass reveals tiles the automap marks as remotely seen); ARCH §4.2 (`Command::Combat` cast/use, `Event::SpellCast { caster, spell, points, components_consumed }`), §4.5 (fight state as built in M4), §8.3 (visibility depth comes from the simulation, so `Sense` feeds the automap not the renderer); PRD D11 (spell points and components, threshold from config), D18 (remote sensing as stale automap knowledge), §7 (magic, items, player knowledge) and §14's component-economy open question; the base pack's eleven spells (`packs/base/data/spells`), 21 items, 21 conditions, four rules files (`casting.ron` exists); `omnis-rules` (`stats::check`, slots) and the M4 fight loop (`combat_menu.rs`, `combat_text.rs`, `apply.rs`). Research with Explore agents first, then Plan agents (one per tack: casting model; items and equipment; remote sensing). Numbers before opinions: measure any new fight content over seeds before the owner plays it.

## Facts the next work will lean on
- Editor research still valid for later: the writer is `omnis-data/src/ron_io.rs`; `omnis-app` reaches data through `omnis_sim::omnis_data`; `omnis-sim` is `no_std`; no teleport command (replays run through `World::new`); `bevy_egui` binds to the first camera unless told otherwise; egui is invisible to the canvas captures. All in the saved plan.
- Wide canvas (`canvas.rs`): `Layout::for_width(width)`; wide iff width ≥ 1716 (viewport centred, roster in a 378-px wing to its left, band under both); `NARROW = for_width(1280)` reproduces every constant. `Raster.origin` in `set`; `Frame::within`, `Frame::push`. `Fit { scale, width, offset }`, `fit((5120,1440)) = {2, 2560, (0,0)}`. Render side: `Res<Layout>` synced in `track_window`; `Spawner`; `pixel::fit_canvas` resizes the canvas target; `ui::upload` resizes the UI image and moves the `UiSprite`.
- Tilesets `viewport: (960, 540)`, `texel_scale: 4`; replays `c2b951a82b883485`, `e4fe51018ea4939d`.
- Sentrux: scan `crates/`, `max_fn_lines 100` incl. tests (`plan::viewport` exactly 100: split before any edit; `parse_args` 91, `loader::resolve` 82, `menu_keys` 75, `socket::serve` 72, `apply::r#move` 72), `max_cycles 0`, no `omnis-app → omnis-data/core` imports.
- Screen dumps: `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored`. Capture: `cargo run -q -p omnis-app -- --seed 1 --no-dev-socket --window huge --script … --screenshot-canvas <png> --settle 40`. Restart the MCP server on a new build before using it.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons. Approved plans are the scope; wider fixes found on the way are follow-ups, or fixed with a test when a test fails on them and named in the commit.
- Verification after the last tree change with fresh `cargo metadata` when workspace contents change. One commit per checked item with the attribution trailer, gated on its checks with `&&`; stage by name, never `add -A`; never push. The owner's files (`docs/background/*`, `.claude/`) are left alone.
- Ask before changing vision documents on drift; approved-by-plan doc edits are flagged in the commit. Check the last push's CI before planning the next milestone. First content is measured before the owner plays it. A display decision is measured on the owner's primary display.
- Dependencies: exact pins, 30 days old, N-1 for non-Bevy crates matching Bevy's version, Socket scores read before adding (the owner overrides a concern only when the newer version scores better).

## Open items and follow-ups
- Push, PR, CI. ARCH to v0.3 if the owner wants it in the PR branch. The vacant M5 number.
- Horizons from the wide canvas: fractional scaling for windowed classes; portraits, conditions, inventory in the wing; a multi-column log; quantising `fit.width` to a cell multiple if drags churn.
- Earlier follow-ups unchanged: raw `sim:message:*` keys in the log; the no-default-features clippy of test targets; `Line::short`/`SHORT_CELLS` deletion; a larger bitmap font; a scrollable log; word labels on the pad; `texel_scale 1`; saving from the pause overlay mid-fight; F5/F9 in a fight; healing downed members outside combat (M7); item loot and encounter budgets (M9); monster spellcasting and multiattack; `Surprise::Monsters`; harder placements once the party can rest and level; `screen.text` op; localized event text; CC0 art; finesse weapons; a `dead` condition; floating damage numbers; `--no-autostart` menu captures; curved pad arrows; hover moving the keyboard cursor; minimap click; `.omnis/mcp.log` rotation; rolled scores, hirelings, save list, in-game difficulty changes, portraits/icons, `depends` ranges, inns, Settler background, equipment slots. Socket re-audit of rhai 1.26.1 due 2026-10-10. The unused `bevy_egui = "=0.41.1"` workspace pin stays for the editor.
