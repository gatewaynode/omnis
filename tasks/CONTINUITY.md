# Continuity notes

Written 2026-09-13 after M3b "Canvas UI: bitmap font, frames, mouse" and the owner's confirmation of the direction, before M4 planning. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). World is Toel (`docs/background/introduction.md`). `PRD.md` v0.3 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md` milestones. M0–M3 done; M3b (the UI pass the owner asked for after seeing the M3 screens) is code-complete.
- Branch **`m3-tasks`**. After the M3 chain (`5d7efb9` docs, `995c22d` continuity): `20f55e6` layout; `dd281df` font and raster; `86836b5` widgets, screens, panels; `31e8292` cursor, startup fit, 1280×720; `26dbda6` UI plugin and pointer tests; `67233f8` raster upload, text nodes gone; `a081cdb` Cargo `ui` feature off, `widget.rs` split for Sentrux, docs. Tree clean. Nothing pushed; the owner opens PRs.
- **Verified**: 170 tests (2 ignored by design); clippy both app configs; fmt; `lint-sim`; `check-duplicates` (no new duplicates, app tree 328 → 323 crates); `validate packs/base packs/test`; golden replay; Sentrux rules pass, signal 7039. Seen by eye: `--screenshot-canvas` now shows the HUD, pad, and band; the screen dump shows title, new game, creation, pause, explore reading well at 4×.
- **Direction confirmed 2026-09-13.** The owner first read the canvas UI as "rebuilding a UI toolkit from scratch", then, after numbers (toolkit part about 800 lines and finished; the screens are content any client needs; M4–M7 need about four small helpers: a scrolling list ~150 lines, a table row helper ~80, word wrap ~40, a modal box ~60) and a night's sleep, accepted it: the sim, rules, protocol, and menu models are client-neutral, so other UI clients (egui, terminal, web) can sit beside this one later. Build path: keep the canvas UI, add those helpers only when a milestone needs them, no egui in the game.
- **Next: plan M4 "Combat" in plan mode** (`tasks/TODO.md` M4). The owner's acceptance run by mouse is still open and can happen any time: `cargo run -p omnis-app -- --window 1280x720` (the default is now 1280×720, so `--window` is optional). Title: click a row. New game: click the seed field and type; click `<` `>` on Saving and Permadeath; click Start. Creation: click the name field and type; `<` `>` on race, class, background, alignment, and each score; click a skill's `[ ]`; click Add member (up to six); click Begin. Explore: the pad under the minimap (turn, forward, turn / sidestep, back, sidestep / USE) or the keys; the band shows the party (front row left, back row right), the message line above, help below; Esc pauses (Resume, Quit to title, Quit by click or keys). Hover outlines the widget under the pointer; the keyboard cursor is the marker and highlighted text. Expect tuning requests: colours, the dense creation screen, glyph shapes, the pad's arrow labels.
- M4 "Combat" inputs already in place: base pack holds goblin, skeleton, giant rat with attacks (`packs/base/data/monsters`); `RollTrace` exists; `Party` has marching order with `front_row` from `rules/creation.ron`; the band draws hp in ALERT below a quarter and has a `condition` initial column; the M0 check-in says M4 uses generated silhouettes until the owner sources CC0 monster art. The combat screen will be the first consumer of the list helper (monster stacks, action row, roll math as text) on the 40×16 menu cells or over the viewport.

## Decisions and facts easy to get wrong after a compact
- Owner decisions for M3b (2026-09-12, after numbers on four directions incl. `bevy_egui` around the canvas: +17 crates, 2 duplicate versions, antialiased text only): all game UI on the 320×180 canvas from a self-authored 5×7 font in 6×8 cells (53×22), 1-px frames, no font asset, no new crates; mouse with hover outlines, clickable rows, arrows, checkboxes, buttons, a movement pad, selectable party rows; party across the bottom band one line each. `bevy_egui` stays editor-only (ARCH A11 corrected). `bevy` features are now `["2d", "png"]`.
- Module graph (Bevy-free unless noted): `layout` (rects, regions, `window_scale` floors, `window_to_canvas`), `font` (glyph art → `const fn` bitmaps), `raster` (RGBA buffer painter), `widget` (palette, `PadButton`, `WidgetId`, `Part`, `Kind`, `Widget`, `Hit`, `hit`, `Frame`, `PadState`), `panels` (`Hud`, `MemberRow`, `Message`, `Band`, backdrop, hud, pad, band), `screens` (title, new game, creation, pause on the viewport's 40×16 cells), `screen` (`Target`, `click`, `Menu`, `View`, `compose`), `menu` (state machines, unchanged behaviour; `lines()` now test-only). Bevy: `cursor` (`WindowSize`, `Pointer`, `UiSet::{Cursor, Dispatch, Model, Draw}`), `ui` (`UiFrame`, `UiClick`, `Selected`, `MessageLine`, `UiImage`, `event_text`, compose + upload), `menus` (`Screens`, `Where::screen() -> Active`, click gathering), `input` (`map_pad`). `widget` sits below `screens`/`panels`/`screen`: Sentrux forbids cycles, and it also reads `bevy::input::mouse` as an import of a `tests/mouse.rs`, hence `tests/pointer.rs`.
- Canvas regions: viewport (0,0,240,135); menu text cells at `(1 + 6c, 8r)`, 40×16; right column x 240..320: minimap (248,8,64,64), HUD lines at (241, 74/82/90) 13 cells, pad (244,100,76,35) with buttons 24×11 at x 244/270/296, y 100/112, USE (244,124,76,11); band (0,135,320,45): message (4,137), rows y 146/154/162 at x 4 (front) and x 164 (back), 26 cells, help (4,171). Overlay automap clipped to (8,8,224,119) through `automap_window`. UI sprite z 50 above viewport 0.5–2, minimap 10, overlay 20.
- Click rule: a click on `Row(r)` sets the model's cursor to `r`, then presses Left/Right (arrow part of a Choice), Enter (Button, Toggle), or nothing (TextField body, Choice body). `Skill(i)` sets cursor 11 and `skill_cursor` then Enter. Hit-testing uses the previous frame's widgets, so a click takes two `app.update()`s like a key. Pad clicks map to commands only in Explore; the pad is drawn dim otherwise and hidden without a world.
- Party rows: `{name:<10} {hp:>3}/{hp_max:<3} {sp:>2} {c}` (23 of 26 cells), name cut to 10 chars, non-ASCII → `?`; during creation `{name:<10} {class}`. Front row fills the left column (up to 3), the rest the right; slots past six are not shown.
- Headless test facts: `ButtonInput::clear()` keeps the key pressed, so a second press of the same key is not a new press; use `reset_all()`. Tests set `Pointer.canvas` directly (the pointer system only overwrites it on `CursorMoved`) and write `MouseButtonInput`; a `Window` + `WindowCreated` message drives the size. `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored` then `sips -s format png x.ppm --out x.png` and `sips -z 720 1280` to view.
- Everything from M3 still holds: `Command` is not `Copy`; `SAVE_SCHEMA = 2` with the v1 migration; any `packs/test` or sim change needs `cargo test -p omnis-sim rebaseline -- --ignored` in the same commit; default pack lists are `packs/base` then `packs/test`; rules files are `slots`/`values`/`tables`; rhai 1.26.1 facts (one-argument `set_max_expr_depths`, strings refused by the AST walk); RON fixed arrays are tuples; untagged `Reply` keeps `Value`/`Done` last; `rules.set` is a host op; captures: `cargo run -q -p omnis-app -- --seed 1 --window 1280x720 --no-dev-socket --script forward,forward --screenshot-canvas <png> --settle 40` (now shows the UI; `--screenshot*` forces autostart, so the menus are not capturable that way yet).
- Sentrux: scan `crates/`; `max_fn_lines 100` applies to tests; `max_cycles 0`. Editing: read the region before patching (LESSONS); rustfmt rewraps `use` blocks and long calls; `echo =====` fails in this zsh.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons (`LESSONS.md`). Approved plans are the scope; wider fixes found on the way are reported as follow-ups.
- Verification runs only after the last tree change; fresh `cargo metadata` when workspace contents change. Commit per checked item with the attribution trailer; stage files by name, never `add -A`; never push.
- Ask before changing vision documents on drift; factual corrections are flagged in the commit and the report (ARCH §8.1, §8.2, §8.4, A11, §13 this pass).

## Open items
- Owner acceptance by mouse; tuning after the first look (palette, creation density, glyphs, pad labels). The M1 note asked for a 3840×2160 default window; the approved M3b plan set 1280×720 because the OS clamped 4K on smaller screens; the owner may want it back on a 4K display.
- Follow-ups: the viewport's own horizon-band sliver at x 230–239 (M2 note); event messages show raw `sim:message:*` keys; a `screen.text` dev-socket op so the MCP can read a screen's widgets; `--no-autostart` menu captures; curved arrow art for the pad; hover moving the keyboard cursor; a minimap click to toggle the overlay.
- `.omnis/mcp.log` grows with every request; rotate or truncate.
- Earlier horizons unchanged: rolled scores, hirelings, a save list for Load, difficulty changes in-game, portraits and icons, `depends` version ranges, inns (Relief/InnOnly refuse every save until M7), the Settler background, equipment slots, CC0 monster and portrait art before M4. Socket re-audit of rhai 1.26.1 due 2026-10-10.
