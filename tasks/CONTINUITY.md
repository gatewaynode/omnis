# Continuity notes

Written 2026-09-13 with M4 "Combat" complete and accepted by the owner (the walkthrough to the rats at (3, 8) was played and verified). Nothing is pushed yet. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). `PRD.md` v0.3 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md`. M0–M3b merged (PR #4). **M4 done on branch `m4-tasks`**, every item ticked, the review and the acceptance written in the TODO's M4 block. The owner pushes and opens PRs; never push.
- M4 commits: sim half `7498554`…`6bdaa4e`; app half `24fa0e8` (menus, text), `aca248d` (screens), `615edda` (silhouettes), `e694e81` (wiring, `CombatPlugin`, script `party`), `7445a60` (tests; the notice bug and the quit-frame fix), `1f07d8d` (horizon sliver), `89e9f3a` (ARCH §4.2/§4.5/§8.1/§9.3/§11 as built, approved-by-plan drift; status line left at v0.2 for the owner); after the owner's first play: `6168794` (`surprise` rules value, 0 in base; rats-only test dungeon; goldens rebaselined), `be15b9f` + `8cbbe1c` (the defeat modal shows the last three log lines, box 48,24,144,80), `7fc7d22` (lesson, review addendum), then the acceptance docs commit.
- **Verified at `8cbbe1c`** (no code changed after it): fresh `cargo metadata`; fmt; clippy workspace `--all-targets -D warnings` and `-p omnis-app --no-default-features`; `cargo test --workspace` 228 passed, 0 failed, 4 ignored (two rebaselines, the capture, the screen dump); `lint-sim` (+ self-test) clean; no new duplicates; `validate packs/base packs/test` (6 monsters, 16 conditions, 13 slots); replays `walk.ron` 30 commands → `a5d7c1b136194a28`, `fight.ron` 46 → `af4b66f5ba0f5926`; Sentrux rules pass, signal 7025. The owner played the walk in the window and accepted it.
- **Next**: the owner pushes `m4-tasks` and opens the PR. **Check that push's CI before anything else** (LESSONS 2026-09-13); a red build is the first task. Then plan M5 (editor v1, `bevy_egui`) in plan mode: read the TODO's M5 line, ARCH §8.4 (editor) and §6.1 (pack layout; the writer is the loader's code path), PRD D4. The MCP server the editor session holds predates M4: restart it to see `combat_get` (18 tools) before using it.

## Facts the next milestone will lean on
- Simulation API for clients: `omnis_sim::{apply, combat_view, bribe_cost, Mode::{Explore, Encounter, Combat}, ModeKind}`; the app imports data/core types through `omnis_sim::omnis_data` / `omnis_core` re-exports only. `World.mode` is not `Copy`; `Status.mode` is a `ModeKind`. Save schema 3 (v1→v2→v3; fixture `tests/saves/v2.ron` loads with `force`).
- Rules are data: `packs/base/data/rules/combat.ron` (13 slots, values `monster_front_stacks 2`, `death_save_dc 10`, `combat_round_minutes 1`, `surprise 0`, table `run_dc`). `Rules::insert_value` sets a value at runtime (tests use it to turn surprise on). Rust rolls every die, Rhai adds and compares.
- Test dungeon (`packs/test/data/maps/dungeon.ron`): rats only — 2 hostile once at (3, 8), 1 wary at (9, 8), 1 friendly once at (15, 8); random table 3% (rat 1d2 weight 3, 1 wary rat weight 1); the meadow's table is 0%. The goblin and skeleton test monsters exist for unit fixtures (`combat_menu::tests::facing_goblins` builds them by name). Any `packs/test` or sim change needs `cargo test -p omnis-sim rebaseline -- --ignored` in the same commit; the golden walk (`golden_walk()`) meets rats on its thirteenth step and fights them with an empty party, `walk()` stays pure for the determinism test.
- App structure (ARCH §8.1 as built): Bevy-free models in `menu.rs` (905 lines), `combat_menu.rs`, `combat_text.rs`, `combat_screen.rs`, `actors.rs`, `screens.rs`, `screen.rs`, `panels.rs`, `widget.rs`, `plan.rs`, `layout.rs`; plugins `sim`, `input`, `cursor`, `pixel`, `assets`, `viewport`, `menus`, `combat`, `ui`, `dev`, `socket`. `PlayState::{CreateParty, Explore, Encounter, Combat, Paused, Defeat}` follows `World.mode` (`for_mode`, `follow_mode`); commands apply when `unpaused` and the world exists. New models go in new files (`menu.rs`, `screens.rs` 624, `screen.rs` 541 are near their limits).
- Headless test kit: `crates/omnis-app/tests/common/mod.rs` (`ui_app`, `ui_app_saving_to`, `Seen` collector after `SimSet::Publish`, pointer/key/click helpers, `place`, the menu flows by mouse). A click or key takes two `app.update()`s, a state change one more. Never read `Messages<M>` directly in a test. `ButtonInput::clear()` keeps a key pressed (`reset_all()`). Taking `&mut` on a `ResMut` marks it changed even without a write (`Res::is_changed` is then always true). Bevy 0.19 `SystemCondition`: use `.and_then`/`.or_else` (`.and`/`.or` are deprecated there).
- Dev script (`--script`): the simulation's words plus `map`, `save`, `load`, `party` (a stock human fighter with the standard array); a capture: `cargo run -q -p omnis-app -- --seed 1 --no-dev-socket --script party,party,forward,…,fight,attack --screenshot-canvas <png> --settle 40`. Screen dump: `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored` (PPM; `sips -s format png` converts).
- Sentrux: scan `crates/`, rules `max_fn_lines 100` incl. tests and `max_cycles 0` (the free tier checks a subset: measure long functions by hand with an awk pass over `fn` bodies, or split anything near 100), no `omnis-app → omnis-data/core` imports. rustfmt rewraps `use` blocks, long calls, and test literals: read the region before patching; anchor big replacements on function names, never on a span between two tests (a whole test was clobbered once this session).

## Working agreements observed
- Numbers before opinions (measure over seeds before proposing content or rule changes); both directions of any hybrid; v1 choices are horizons. Approved plans are the scope; wider fixes found on the way are reported as follow-ups, or fixed with a test when a test fails on them and named in the commit.
- Verification runs only after the last tree change; fresh `cargo metadata` when workspace contents change. Commit per checked item with the attribution trailer; stage files by name, never `add -A` (the owner's `docs/background/introduction.md`, `docs/background/backgrounds_and_skills.md`, and `.claude/` are theirs; leave them alone); never push.
- Ask before changing vision documents on drift; approved-by-plan doc edits are flagged in the commit. Check the last push's CI before planning the next milestone. First content is measured before the owner plays it; a mechanic that can end a fight before the first turn ships off by default (LESSONS 2026-09-13).

## Open items and follow-ups
- The push and PR (owner). If the owner wants the ARCH status bumped to v0.3 for M4, do it in the PR branch.
- Horizons from M4 (in the TODO review): scrollable roll log; saving from the pause overlay mid-fight; F5/F9 in a fight (input is Explore-only); healing downed members outside combat (M7); item loot and encounter budgets (M9); monster spellcasting and multiattack; `Surprise::Monsters`; surprise back on as data once the fights have been played; harder placements back once the party can rest and level; `screen.text` op; localized event text (client English for now); silhouette shapes and depth, then CC0 art; finesse weapons; a `dead` condition is required for members to die; floating damage numbers (ARCH §8.1 still describes them).
- Earlier follow-ups unchanged: raw `sim:message:*` keys in the message line; `--no-autostart` menu captures; curved pad arrows; hover moving the keyboard cursor; minimap click; `.omnis/mcp.log` rotation; the 3840×2160 default if wanted; rolled scores, hirelings, save list, in-game difficulty changes, portraits/icons, `depends` ranges, inns, Settler background, equipment slots. Socket re-audit of rhai 1.26.1 due 2026-10-10.
