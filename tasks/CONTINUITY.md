# Continuity notes

Written 2026-09-19 after the M6 plan was approved. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). `PRD.md` v0.4 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md`. M0–M4 merged (PRs #4, #5). The owner pushes and opens PRs; never push.
- Branch `m5-tasks` (remote at `024772c`, no PR, CI is PR-triggered, main green) holds the M4 docs, the display rework, the wide canvas, the editor deferral (M5 keeps its heading, marked deferred; plan in `tasks/plans/editor-v1.md`) and now the M6 plan. The tree is clean apart from the owner's `docs/background/*` files (never staged).
- **M6 plan approved 2026-09-19**: full text at `/Users/john/.claude/plans/nested-growing-bear.md` (outside the repo; the TODO M6 block carries the checkable items). 19 commits: shared (docs ✔, data, content R, rules, schema 4 R), M6a casting (6–12, owner plays), M6b items (13–16, owner plays), M6c sensing (17–19, owner plays). Two rebaselines only (content, schema 4); every later commit must touch neither `packs/` nor the World's serialized shape.
- Owner decisions 2026-09-19: all eleven spells castable; three acceptance points; `Party.gems` kept and deprecated; a debug menu (backtick/F1, explore and fight, add items, set values) gated by `Settings.devtools`; potion in the acolyte kit; Explorer (Survival, Perception stand-ins; spyglass, map-making kit) and Warden (Arcana, Investigation) backgrounds; GFDL backgrounds later as a separate pack (TODO Phases 2–5). Deferred milestones keep their number (LESSONS 2026-09-19).
- **Next**: commit 2 of the plan, "Data": `omnis-data` spell `effect`/`reach` (`SpellEffect` with Attack, AutoHit, Save, Heal, Buff, Reaction, Light, Utility; `BuffOn`), item `use_effect`/`consumable`/`description`, `Slot` + `Item::slot()`/`two_handed()`, `sense.rs` (`SenseSource`, `Geometry::Ray`, `Fidelity`, `Persistence`), `limits::MAX_SENSE_RANGE 64`, validations, the loader's component-threshold check, bad-pack rows, CLI schema examples, round-trip tests. No pack change in that commit (no rebaseline). Then commit 3 (content, R) with the pin `(4, 4, 3, 24, 16, 11, 3, 19)`.

## Facts the next work will lean on
- Casting: `stats::spell_point_pool`/`spell_cost` exist (called only at creation); `casting.ron` value `component_threshold` is read by nothing; `Roller::take/store` copies the `combat` stream; `hurt_stack` hits index 0; `set_condition` is private in `combat/resolve.rs`; `Stack` has no conditions; monsters carry abilities and damage lists. `advance()` is the only clock writer and a round is one minute, so effect expiry is an absolute party-clock minute.
- Items: `Character.equipment` flat "counts as worn"; `Party.inventory` written only by `bury`; items intern in id order (new files renumber `ItemId`s). Pins: `load_base_pack.rs:31-39`, `chain_mail` shape, `bad_packs.rs` wording, MCP tool count 18 in four places, hand-written `Command` schema `oneOf` = 6.
- Sensing: `Known { terrain, walls, doors, layers, seen_at }`, layer bits 1/2/4, `record` overwrites; `line_of_sight` private; `plan::automap` never reads `seen_at`; `compact_visible` in the MCP bridge.
- App: free keys C, I, L, F1, backtick; `ACTIONS: [&str; 4]`; `combat_text.rs` 927 lines (new lines in new files); `menu_keys` 75 (extract `route_key`); `plan::viewport` exactly 100 (never touched). Sentrux near-cap: `load_one` 92, `create` 84, `game_tools` 99, `party_view` 44.
- Wide canvas facts unchanged (`canvas::Layout::for_width`, wide iff width ≥ 1716; tilesets `viewport: (960, 540)`, `texel_scale: 4`). Replays `c2b951a82b883485`, `e4fe51018ea4939d` until commit 3.
- Screen dumps: `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored`. Restart the MCP server on a new build before using it.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons. Approved plans are the scope; wider fixes are follow-ups, or fixed with a test when a test fails on them and named in the commit.
- Verification after the last tree change with fresh `cargo metadata` when workspace contents change. One commit per checked item with the attribution trailer, gated on its checks with `&&`; stage by name, never `add -A`; never push. The owner's files (`docs/background/*`, `.claude/`) are left alone.
- Ask before changing vision documents on drift; approved-by-plan doc edits are flagged in the commit. Check the last push's CI before planning the next milestone. First content is measured before the owner plays it (the M6a harness). A display decision is measured on the owner's primary display. A deferred milestone keeps its number.
- Dependencies: exact pins, 30 days old, N-1 for non-Bevy crates matching Bevy's version, Socket scores read before adding.

## Open items and follow-ups
- Drift to raise (listed in the plan): PRD §8.3 action economy and reactions vs one action per turn and the opt-in shield; PRD §8.1 vs the owner's new proficiencies; ARCH §4.2 planned command names vs as built; ARCH §9.3 "generated" schemas are hand-written; ARCH §4.1 `rng`; `world.rs:149` Relief in M6; the editor still in Phase 1 (PRD D4/§13, ARCH §16); PRD §11.1 Feathers; two D20 rows; §14 depth 4.
- Push, PR, CI. ARCH to v0.3. Horizons from the plan's last section; earlier follow-ups unchanged (raw `sim:message:*` keys; `Line::short` deletion; scrollable log; word labels on the pad; `texel_scale 1`; saving from the pause overlay mid-fight; item loot and encounter budgets (M9); monster spellcasting; `Surprise::Monsters`; `screen.text` op; localized event text; CC0 art; finesse weapons; floating damage numbers; `.omnis/mcp.log` rotation). Socket re-audit of rhai 1.26.1 due 2026-10-10. The unused `bevy_egui = "=0.41.1"` pin stays for the editor.
