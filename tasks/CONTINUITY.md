# Continuity notes

Written 2026-09-19 after M6b (items and equipment) was built and handed to the owner for acceptance. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). `PRD.md` v0.4 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md`. M0–M4 merged (PRs #4, #5). The owner pushes and opens PRs; never push.
- Branch `m5-tasks` holds the M4 docs, the display rework, the wide canvas, the editor deferral (M5 keeps its heading, marked deferred), the M6 shared commits, M6a (casting, effects, the debug menu, the fight picker, the explore cast menu; owner acceptance passed 2026-09-19), the entry-points step (Save/Load/Character sheet on the pause overlay, the tool pad, the character sheet; owner acceptance passed 2026-09-19), the owner's own commit `4780d66` (background materials under `docs/background/`), and **M6b**: sim item commands `a85e693`, ops/MCP views `e877fa9`, the inventory overlay `2587ebb`, and the fight's item picker with the M6b docs (the head). The owner pushed the branch on 2026-09-19 and is testing CI with a merge they will reject; ask what CI said before planning past M6.
- **M6 plan** (approved 2026-09-19): full text at `/Users/john/.claude/plans/nested-growing-bear.md`; the TODO M6 block is the checklist. Done: commits 1–16 and E1–E3. Remaining: **M6c** sensing (17 sim `layer::REMOTE`, layer-aware `Automap::record`, `visibility::ray`, `sense.rs`, `Event::Sensed`, the `Use` Sense arm replacing today's `NotUsable`; 18 MCP `compact_tiles` for `Sensed`, the LOOK button and L send the first carried sense item's `Item(Use)`, `sense_text.rs`, `plan::automap` REMOTE outline + pixel test, flip `ToolButton::Look` in `ui::tool_states` and `input::tool_for`; 19 docs and the milestone review, owner acceptance). No commit may touch `packs/` or the World's serialized shape (the golden replays would move).
- **Next: owner acceptance of M6b by mouse** (open ITEMS, chain mail off and on, stow the potion and take it back, give bolts, drink a potion on a hurt member, a potion from USE in a fight). After acceptance (or fixes), start commit 17.

## Facts the next work will lean on
- Items as built: `omnis-sim/src/items.rs` (`ItemCommand`, `UsePlan`, `validate_use(world, data, own, item, target, fight)`, `use_item`, `transfer`, `drop_worn`; the `items` stream); `CombatCommand::Use { item, target }` → `Plan::Use`; events `Equipped`, `Unequipped`, `ItemMoved { item, count, from: ItemPlace, to }`, `ItemUsed { member, item, target, consumed }`; rejections `UnknownItem`, `NotInStores`, `NotCarried`, `NotEquippable`, `HandsFull`, `SlotEmpty`, `NotUsable`, `NotUsableHere`, `TargetDead`, `ZeroCount` (worded in `Rejection::fmt_items`); words `use-item-N`, `use-item-N-mM`, `item`. `ops::{ItemView, member_view, item_views, effect_names}`; `MemberView.{equipment, equipped, effects}`, `PartyView.{inventory, effects}`. MCP `Command` schema `oneOf` **9**, combat arms **5**, `item_schema` 6, `dev_schema` 12, tool count 18.
- App: `PlayState::Inventory`, `ShellCommand::Inventory` (I key, `ToolButton::Items`), `Active::Inventory`, `Screens.inventory`, `Target::Inventory` (a tab or row click only picks, in `screen::click`), `Menu::Inventory`, `HELP_INVENTORY`, `ui::overlay_for` (the Cast/Debug/Sheet/Inventory arms of `menu_for`); `inventory_menu::{InventoryView, inventory_view, InventoryAction (E/U/S/T/G), InventoryMenu, InventoryIntent}`, `inventory_screen::inventory` (tabs `Row(0..panes)`, rows `Row(panes+i)`, buttons `Action(i)`), `inventory::InventoryPlugin` (in `main.rs` and `tests/common`), `item_text::item_line` (chained in `combat_text::event_line`); `text.rs` holds `Names` (now with `item`), `Line`, `LONG_CELLS`, `SHORT_CELLS`, `trace_math`, `faces`, imported downward by `combat_text.rs`, `spell_text.rs`, `item_text.rs` (Sentrux flagged the cycle the item lines made). Fight: `CombatMenu.use_picker`, `FightView.usable: Vec<UseRow>`, `use_menu::{UseRow, use_rows}`, `combat_screen::use_picker` with `WidgetId::Item(position)`, `ACTION_USE` live. Screen dumps gain `inventory` and `combat_use`.
- Casting as built: `combat/cast.rs`, `casting.rs`, `effects.rs`, `checks.rs::roll`, `combat/reaction.rs::try_shield`, `utility.rs::open_door_ahead`, `visibility::depth`. Dev: `DevCommand` (12), `Settings.devtools`, `combat::resume`/`turn::settle`; the debug menu opens on the backtick only.
- Pins: base tuple `(4, 4, 3, 24, 16, 11, 3, 19)`; replays walk `238033710167572364`, fight `7317777168019603128`; `SAVE_SCHEMA 4`; pause widgets `Pause::ITEMS.len()`; `tests/pointer.rs` pause rows: Resume 0, Save 1, Load 2, Character sheet 3, Quit to title 4.
- Sentrux: `scan` (or `rescan`) then `check_rules` before every commit; `max_fn_lines 100` incl. tests; near the cap `debug_menu::adjust` 93, `debug_screen::row_text` 91, `combat_text::wound_line` 89, `tests/inventory.rs` first test 94, `dump_screens` ~92. `scripts/verify.sh` is the gate: run it unpiped and check its status. Test count 337+ at the gate.
- Screen dumps: `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored`. Measurement: `cargo test -p omnis-sim --test measure -- --ignored --nocapture`. Restart the MCP server on a new build before using it.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons. Approved plans are the scope; wider fixes are follow-ups, or fixed with a test when a test fails on them and named in the commit. A deferred milestone keeps its number. Every action gets a button before a key; no function keys; save by mouse is verified before a hand-over.
- Verification after the last tree change: `scripts/verify.sh > log 2>&1 && …`, never piped. One commit per checked item with the attribution trailer; stage by name, never `add -A`; never push. `.claude/` is never staged. Patch a file as it is after `cargo fmt` (anchors reflow; replace whole functions when in doubt; a python edit script is parsed whole and every anchor checked before anything is written; a script kept in the scratchpad is rerun after an anchor fix).
- Ask before changing vision documents on drift; approved-by-plan doc edits (the "as built" paragraphs in ARCH §4.5) are flagged in the commit. Check the last push's CI before planning the next milestone. First content is measured before the owner plays it. A display decision is measured on the owner's primary display.
- Dependencies: exact pins, 30 days old, N-1 for non-Bevy crates matching Bevy's version, Socket scores read before adding.

## Open items and follow-ups
- Drift to raise with the owner (not edited): PRD §8.3 action economy and reactions; PRD §8.1 vs the owner's crawler abilities; ARCH §8.1's plugin list gains `DebugPlugin`, `SheetPlugin`, `InventoryPlugin`; ARCH §9.3 says schemas are generated (hand-written in `omnis-mcp/src/schema.rs`); ARCH §4.1 `rng` vs `rngs`; `world.rs:149` Relief items in M6 (M7); the editor in Phase 1 (PRD D4/§13, ARCH §16); PRD §11.1 Feathers; two D20 rows; §14 depth 4. ARCH to v0.3.
- M6b horizons: counts other than one per move; item details; a seventh member's tab; weight limits; proficiency and strength penalties; a `doff_armor_minutes` value; the fight picker's reasons beyond "not here". Earlier horizons unchanged: the sheet's attack line, death saves, traits pages, scrolling, age as a rules function; a seventh pause item needs a second column, a seventh tool a wider column; MAP in a fight; no rest until M7; `--no-devtools`; the measurement policy is a fixed heuristic; raw `sim:message:*` keys; `Line::short` deletion; scrollable log; word labels on the pad; `texel_scale 1`; saving from the pause overlay mid-fight; loot and encounter budgets (M9); monster spellcasting; `Surprise::Monsters`; `screen.text` op; localized event text; CC0 art; finesse weapons; floating damage numbers; `.omnis/mcp.log` rotation. Socket re-audit of rhai 1.26.1 due 2026-10-10. The unused `bevy_egui = "=0.41.1"` pin stays for the editor.
