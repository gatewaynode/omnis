# Continuity notes

Written 2026-09-19 after the owner accepted M6a and the entry-points step (E1–E3). The next work is M6b commit 13. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). `PRD.md` v0.4 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md`. M0–M4 merged (PRs #4, #5). The owner pushes and opens PRs; never push.
- Branch `m5-tasks` (remote at `024772c`, no PR, CI is PR-triggered, main green) holds the M4 docs, the display rework, the wide canvas, the editor deferral (M5 keeps its heading, marked deferred), the M6 shared commits, M6a (casting, effects, the debug menu, the fight picker, the explore cast menu; the owner's play passed magic in combat), and the **entry-points step**: Save/Load/Character sheet on the pause overlay with no function keys (`5039bc0`), a Sentrux debt refactor (`c1f26b7`), the tool pad (`362960f`), and the character sheet (`5c62326`). The tree is clean apart from the owner's `docs/background/*` files (never staged).
- **M6 plan** (approved 2026-09-19): full text at `/Users/john/.claude/plans/nested-growing-bear.md`; the TODO M6 block is the checklist. Done: commits 1–12 and E1–E3. Remaining: **M6b** items and equipment (13 sim `Command::Item` + `CombatCommand::Use`; 14 ops/MCP kit views; 15 inventory overlay on the ITEMS button and I; 16 Use in the fight picker + docs + acceptance), **M6c** sensing (17 sim `layer::REMOTE`, `visibility::ray`, `sense.rs`, `Event::Sensed`; 18 MCP compaction + the LOOK button and L + `sense_text.rs` + automap outline; 19 docs + acceptance). No commit may touch `packs/` or the World's serialized shape (the golden replays would move).
- **Owner acceptance 2026-09-19**: M6a (magic in combat) and the entry points (save/load works, the sheet opens, all buttons pass) both passed. **Next: start commit 13** (`crates/omnis-sim/src/items.rs`: `ItemCommand { Equip, Unequip, Give, Stow, Take, Use }`, validate-then-mutate, events `Equipped/Unequipped/ItemMoved/ItemUsed`, `don_armor_minutes`/`use_item_minutes`, the potion through `party::heal`, `CombatCommand::Use { item, target }`; `tests/items.rs` grows). Commit 15 flips the ITEMS tool state (`ui::tool_states`) and maps `ToolButton::Items` in `input::tool_for`; commit 18 does the same for LOOK.

## Facts the next work will lean on
- Entry points as built: `menu::Pause::ITEMS` six at rows `8 + i` (`PauseAction::{Save, Load, Sheet}`; `menus::Actions.shell`); `sim.rs` runs `shell` whenever a world exists and gates only `apply_commands` on `unpaused`; `layout::{TOOLS, tool_button, TOOL_BUTTONS}`; `widget::{ToolButton, ToolStates, WidgetId::Tool}`; `panels::{framed_button, tools}`; `screen::View.tools`; `ui::tool_states(active, has_world, has_casts, has_members)`; `input::{tool_for, map_tools}`; `ShellCommand::Sheet`, `PlayState::Sheet`, `Active::Sheet`, `Screens.sheet`, `Target::Sheet`, `Menu::Sheet`, `Overlays.sheet`, `HELP_SHEET`; `sheet_menu::{SheetPage, SheetView, sheet_view, SheetMenu, SheetIntent, signed, slot_label}`, `sheet_screen::{sheet, ROW_MEMBER}`, `sheet::SheetPlugin` (in `main.rs` and `tests/common`). Test helpers: `common::Seen.shell` collects shell commands; `common::ui_app_saving_to`.
- Casting as built: `combat/cast.rs`, `casting.rs`, `effects.rs`, `checks.rs::roll`, `combat/reaction.rs::try_shield`, `utility.rs::open_door_ahead`, `visibility::depth`. Items so far: `items::{add_to, take_from, count_of, has_all, consume, item_id}`; `EquipSlot {MainHand, OffHand, Ranged, Body}`, `Character.equipped`, `equip.rs`; `Item { use_effect, consumable, description }`. Dev: `DevCommand` (12), `Settings.devtools`, `combat::resume`/`turn::settle`; the debug menu opens on the backtick only.
- Pins: base tuple `(4, 4, 3, 24, 16, 11, 3, 19)`; replays walk `238033710167572364`, fight `7317777168019603128`; `SAVE_SCHEMA 4`; MCP tool count 18, `Command` schema `oneOf` 8, `dev_schema` 12; pause widgets `Pause::ITEMS.len()`; `tests/pointer.rs` pause rows: Resume 0, Save 1, Load 2, Character sheet 3, Quit to title 4.
- Sentrux: `scan` then `check_rules` before every commit (LESSONS 2026-09-19: the pass was skipped through M6a and four violations piled up); `max_fn_lines 100` incl. tests; near the cap `debug_menu::adjust` 93, `debug_screen::row_text` 91. `scripts/verify.sh` is the gate: run it unpiped and check its status.
- Screen dumps: `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored` (now with `sheet_stats`, `sheet_magic`, `sheet_gear`; `explore` shows the tool pad, `pause` six items). Measurement: `cargo test -p omnis-sim --test measure -- --ignored --nocapture`. Restart the MCP server on a new build before using it.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons. Approved plans are the scope; wider fixes are follow-ups, or fixed with a test when a test fails on them and named in the commit. A deferred milestone keeps its number. Every action gets a button before a key; no function keys; save by mouse is verified before a hand-over.
- Verification after the last tree change: `scripts/verify.sh > log 2>&1 && …`, never piped. One commit per checked item with the attribution trailer; stage by name, never `add -A`; never push. The owner's files (`docs/background/*`, `.claude/`) are left alone. Patch a file as it is after `cargo fmt` (anchors reflow; replace whole functions when in doubt; a python edit script must be parsed whole before it runs, and a failed anchor mid-script leaves the earlier edits applied).
- Ask before changing vision documents on drift; approved-by-plan doc edits are flagged in the commit. Check the last push's CI before planning the next milestone. First content is measured before the owner plays it. A display decision is measured on the owner's primary display.
- Dependencies: exact pins, 30 days old, N-1 for non-Bevy crates matching Bevy's version, Socket scores read before adding.

## Open items and follow-ups
- Drift to raise with the owner (not edited): PRD §8.3 action economy and reactions; PRD §8.1 vs the owner's crawler abilities; ARCH §4.2 `Item(ItemCommand)`/sensing through `Item(Use)`; ARCH §8.1's plugin list gains `DebugPlugin` and `SheetPlugin`; ARCH §9.3 says schemas are generated (hand-written in `omnis-mcp/src/schema.rs`); ARCH §4.1 `rng` vs `rngs`; `world.rs:149` Relief items in M6 (M7); the editor in Phase 1 (PRD D4/§13, ARCH §16); PRD §11.1 Feathers; two D20 rows; §14 depth 4. ARCH to v0.3.
- Push, PR, CI. Horizons: the sheet's attack line, death saves, traits pages, item details, scrolling, age as a rules function; a seventh pause item needs a second column, a seventh tool a wider column; MAP in a fight; the picker's Use row inert until M6b; no rest until M7; `--no-devtools`; the measurement policy is a fixed heuristic. Earlier follow-ups unchanged (raw `sim:message:*` keys; `Line::short` deletion; scrollable log; word labels on the pad; `texel_scale 1`; saving from the pause overlay mid-fight; loot and encounter budgets (M9); monster spellcasting; `Surprise::Monsters`; `screen.text` op; localized event text; CC0 art; finesse weapons; floating damage numbers; `.omnis/mcp.log` rotation). Socket re-audit of rhai 1.26.1 due 2026-10-10. The unused `bevy_egui = "=0.41.1"` pin stays for the editor.
