# Continuity notes

Written 2026-09-19 with M6a built and awaiting the owner's acceptance. Rewrite this file every time it is used.

## Where we are
- Omnis: turn-based first-person grid-crawler RPG, Rust, Bevy 0.19.1 (owner exempts Bevy from the N-1 rule). `PRD.md` v0.4 and `ARCHITECTURE.md` v0.2 are the source of truth; build order follows `tasks/TODO.md`. M0–M4 merged (PRs #4, #5). The owner pushes and opens PRs; never push.
- Branch `m5-tasks` (remote at `024772c`, no PR, CI is PR-triggered, main green) holds the M4 docs, the display rework, the wide canvas, the editor deferral (M5 keeps its heading, marked deferred; plan in `tasks/plans/editor-v1.md`) and the M6 work so far: the shared commits (data, content R, rules, schema 4 R) and M6a (casting in and out of fights, effects, the debug menu, the measurement harness, the fight picker and the explore cast menu). The tree is clean apart from the owner's `docs/background/*` files (never staged).
- **M6 plan** (approved 2026-09-19): full text at `/Users/john/.claude/plans/nested-growing-bear.md`; the TODO M6 block is the checklist. Done: commits 1–12. Remaining: **M6b** items and equipment (13 sim `Command::Item` + `CombatCommand::Use`; 14 ops/MCP kit views; 15 inventory overlay on I; 16 Use in the fight picker + docs + acceptance), **M6c** sensing (17 sim `layer::REMOTE`, `visibility::ray`, `sense.rs`, `Event::Sensed`; 18 MCP compaction + L key + `sense_text.rs` + automap outline; 19 docs + acceptance). No commit after 5 may touch `packs/` or the World's serialized shape (the golden replays would move).
- **Next: the owner plays M6a** (`cargo run -p omnis-app` with the dev build: new game, six drafts or `--script`, a fight: cast every spell once, empty a pool, refill it from the debug menu (backtick or F1, SP row), bless from the back row, switch shield's auto-cast in the picker, light in the dungeon, mage hand on a door, C for the road spells). After acceptance (or fixes), start commit 13: `crates/omnis-sim/src/items.rs` gains `ItemCommand { Equip, Unequip, Give, Stow, Take, Use }` with validate-then-mutate, events `Equipped/Unequipped/ItemMoved/ItemUsed`, `don_armor_minutes`/`use_item_minutes`, the potion through `party::heal`, `CombatCommand::Use { item: u8, target }` (an action; Sense in a fight → `NotUsableHere`); `tests/items.rs` grows.

## Facts the next work will lean on
- Casting as built: `combat/cast.rs` (`check(world, data, own, index, fight, rng)`, `validate`, `pay`, `resolve`, `cast_buff`, `cast_light`), `casting.rs` (explore), `effects.rs` (prune in `advance`, `end_concentration`, `consume`, `clear_next_turn`), `checks.rs::roll` (the only check path), `combat/reaction.rs::try_shield`, `utility.rs::open_door_ahead`, `visibility::depth(world, map)`. Rejections and events listed in ARCH §4.2. `Roller::take_stream(world, name)`.
- Items so far: `items::{add_to, take_from, count_of, has_all, consume, item_id}`; `EquipSlot {MainHand, OffHand, Ranged, Body}`, `Character.equipped`, `equip.rs` (`can_equip`, `equip`, `unequip`, `auto_equip`); `Item { use_effect: Option<UseEffect::{Heal, Sense}>, consumable, description }`; the potion is in the acolyte kit, the spyglass and map-making kit in the Explorer's. `Party.gems` deprecated, never written.
- Dev: `DevCommand` (12 variants), `Settings.devtools`, `combat::resume`/`turn::settle`; the app's `debug_menu.rs`/`debug_screen.rs`/`debug.rs`, `PlayState::{Debug, Cast}`, `Active::{Debug, Cast}`, `ShellCommand::Cast`, `menu_key` maps Tab to `Char('\t')`.
- App fight row: `ACTIONS` six with `ACTION_CAST 1`, `ACTION_USE 2` (inert), `COMBAT_ACTION_COLUMNS [1, 9, 15, 20, 27, 37]`; picker in `spell_menu.rs` (`SpellRow`, `picker_key`, `confirm_cast`, `CastMenu`, `cast_rows`, `cast_screen`); `spell_text.rs` lines; `WidgetId::Spell`; `ui::Overlays`.
- Pins: base tuple `(4, 4, 3, 24, 16, 11, 3, 19)`; replays walk `238033710167572364`, fight `7317777168019603128`; `SAVE_SCHEMA 4` with fixture `tests/saves/v3.ron`; MCP tool count 18 (four places), `Command` schema `oneOf` 8, `dev_schema` 12.
- Sentrux: `max_fn_lines 100` incl. tests (`plan::viewport` exactly 100, untouched), near the cap `debug_menu::adjust` 93, `debug_screen::row_text` 91, `cast::resolve` ~85; files ≲ 1000 (`combat_text.rs` 943, `combat_menu.rs` 779). `scripts/verify.sh` is the gate: run it unpiped and check its status (LESSONS 2026-09-19).
- Screen dumps: `OMNIS_DUMP_SCREENS=<dir> cargo test -p omnis-app --lib dump_screens -- --ignored` (now with `combat_cast` and `debug`). Measurement: `cargo test -p omnis-sim --test measure -- --ignored --nocapture`. Restart the MCP server on a new build before using it.

## Working agreements observed
- Numbers before opinions; both directions of any hybrid; v1 choices are horizons. Approved plans are the scope; wider fixes are follow-ups, or fixed with a test when a test fails on them and named in the commit. A deferred milestone keeps its number.
- Verification after the last tree change: `scripts/verify.sh > log 2>&1 && …`, never piped. One commit per checked item with the attribution trailer; stage by name, never `add -A`; never push. The owner's files (`docs/background/*`, `.claude/`) are left alone. Patch a file as it is after `cargo fmt` (anchors reflow; replace whole functions when in doubt).
- Ask before changing vision documents on drift; approved-by-plan doc edits are flagged in the commit. Check the last push's CI before planning the next milestone. First content is measured before the owner plays it. A display decision is measured on the owner's primary display.
- Dependencies: exact pins, 30 days old, N-1 for non-Bevy crates matching Bevy's version, Socket scores read before adding.

## Open items and follow-ups
- Drift to raise with the owner (not edited): PRD §8.3 action economy (healing word as an action) and reactions (shield opt-in, fires only when it matters); PRD §8.1 vs the owner's Cartography/Orienteering/Mining/Assaying/Refining (Explorer ships with Survival and Perception); ARCH §4.2 still lists `Item(ItemCommand)` as M6b and sensing through `Item(Use)` as M6c; ARCH §9.3 says schemas are generated, they are hand-written in `omnis-mcp/src/schema.rs`; ARCH §4.1 `rng` vs `rngs`; `world.rs:149` says Relief items arrive in M6 (M7); the editor still in Phase 1 (PRD D4/§13, ARCH §16); PRD §11.1 Feathers; two D20 rows; §14 depth 4. ARCH to v0.3.
- Push, PR, CI. Horizons from the plan's last section and the M6a review. Earlier follow-ups unchanged (raw `sim:message:*` keys; `Line::short` deletion; scrollable log; word labels on the pad; `texel_scale 1`; saving from the pause overlay mid-fight; loot and encounter budgets (M9); monster spellcasting; `Surprise::Monsters`; `screen.text` op; localized event text; CC0 art; finesse weapons; floating damage numbers; `.omnis/mcp.log` rotation). Socket re-audit of rhai 1.26.1 due 2026-10-10. The unused `bevy_egui = "=0.41.1"` pin stays for the editor.
