//! `omnis-mcp`: MCP bridge (M2). Until then it exits immediately so `.mcp.json` registration
//! cannot hang a client.
#![forbid(unsafe_code)]

fn main() {
    eprintln!("omnis-mcp: bridge not implemented yet (M2)");
}
